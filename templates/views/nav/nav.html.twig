<nav>
    <ul>
        <li><a href="{{ path('main_home') }}">Home</a></li>
        <li><a href="{{ path('main_test') }}">Test</a></li>
        <li><a href="{{ path('series_list') }}">All TV-SHOW</a></li>
        {% if app.user %}
            <li><a href="{{ path('series_create') }}">Create a TV-SHOW</a></li>
            {% if is_granted("ROLE_ADMIN") %}
                <li><a href="{{ path('season_add') }}">Create a season</a></li>
            {% endif %}
            <li><a href="{{ path('app_logout') }}">({{ app.user.firstname }}) | Logout</a></li>
        {% else %}
            <li><a href="{{ path('app_login') }}">Login</a></li>
            <li><a href="{{ path('app_register') }}">Register</a></li>
        {% endif %}
    </ul>
</nav>
